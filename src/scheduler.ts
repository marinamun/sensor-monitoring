//Log the start of each scan (timestamp, port ID, scan ID)
// Loop through the sensors in each scan sequentially
// Ensure that scans on the same port are executed sequentially while scans on different ports run concurrently
// Use the sleep function to simulate delays for measurements